# The following packages are required for your Streamlit chatbot app
# Do NOT pin asyncio — it's part of Python stdlib and backports can break installs
aiohttp==3.8.6
streamlit==1.28.0
python-docx==0.8.11
PyPDF2==3.0.1

# Optional features (install fine on Cloud; runtime OCR needs system binaries)
pdf2image==1.16.3
pytesseract==0.3.10

# Optional speech-to-text (works locally; Cloud runtime may need ffmpeg)
SpeechRecognition==3.10.0
audio_recorder_streamlit==0.0.6

# Imaging
pillow==10.2.0

# opencv-python==4.9.0.80  # Optional: for advanced video/image processing
# Remove or comment out unused or system-dependent packages for Streamlit Cloud
